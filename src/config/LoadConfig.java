package config;

public class LoadConfig {
    /**
     * Choose the max number of rows to load.
     */
    public static final int maxRows = Integer.MAX_VALUE;
    /**
     * Enlarge the dataset by copying original data.
     */
    public static final int factor = 1;
}
